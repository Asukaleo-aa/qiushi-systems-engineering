---
name: formalization-tools
description: |
  形式化工具：将定性系统描述构造为可计算的耦合矩阵，运行 FSM 状态诊断和假稳态检测。
---

读取并遵循 `formalization-tools/SKILL.md`。

当前任务背景：
$ARGUMENTS

输出要求：
1. 根据用户需求的量化程度，选择 A（定性）/ B（定量）/ C（混合）模式。
2. 如果选择 B 或 C 模式，先运行工具可用性自检。
3. 按照对应工具的输出格式产生结果。
4. 定性模式的所有数值标注"定性推演，未经数值验证"。
