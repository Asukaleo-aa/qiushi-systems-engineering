---
name: workflows
description: |
  跨认知环节串联多个 skill 的标准化工作流——新项目启动/复杂问题攻坚/方案迭代优化。
---

读取并遵循 `workflows/SKILL.md`。

当前任务：
$ARGUMENTS

输出要求：
1. 判断任务匹配哪条工作流（启动/攻坚/迭代）。
2. 按工作流定义的顺序调用各环节 skill。
3. 在步骤间传递规定格式的产出物。
4. 达到终止条件时宣告工作流完成。
