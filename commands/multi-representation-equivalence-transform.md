---
name: multi-representation-equivalence-transform
description: |
  多表示等价变换：时域↔频域↔状态空间↔几何，在不同表示间切换找到最简解法。
---

读取并遵循 `multi-representation-equivalence-transform/SKILL.md`。

当前问题：
$ARGUMENTS

输出要求：
1. 列出当前使用的表示形式。
2. 判断是否适合做变换——当前表示下是否"卡住了"。
3. 选择目标表示并执行等价变换。
4. 在目标表示下求解。
5. 将结果翻译回原始表示。
